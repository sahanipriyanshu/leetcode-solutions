            
            if (i > start && candidates[i] == candidates[i-1]) {
                continue;
            }
    
        for (int i = start; i < candidates.size(); i++) {
            res.push_back(comb);
            return;
        }
        }
    
        if (target == 0) {
